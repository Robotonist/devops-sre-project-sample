# Local Docker Compose configuration for v0.1.
# Compose derives DATABASE_URL and REDIS_URL for application containers.
APP_NAME=Ops Appliance
ENVIRONMENT=development
POSTGRES_DB=ops
POSTGRES_USER=ops
POSTGRES_PASSWORD=change-me
